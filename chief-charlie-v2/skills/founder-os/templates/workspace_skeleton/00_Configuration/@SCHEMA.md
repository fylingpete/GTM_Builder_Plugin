# Schema

> Auto-generated by Founder-OS onboarding on {{DATE}}.

## Purpose

Quick reference for the founder's data files and where state lives.

## Files

| File | Type | Purpose |
|---|---|---|
| `dashboard_data.json` | object | Single source of truth for current state |
| `decisions_learnings.jsonl` | append-only stream | All decisions + learnings (universal) |
| `weekly_checkin_log.jsonl` | append-only stream | Check-In history |
| `pmf_survey_responses.jsonl` | append-only stream | PMF only |
| `pmf_score_history.jsonl` | append-only stream | PMF only |

## Read/Write Rules

- **Current state →** read/write `dashboard_data.json` only.
- **History →** append to the relevant `*.jsonl`.
- **Never** query `*.jsonl` to find current state. Use `dashboard_data.json`.

## Schemas

See `templates/dashboard_data.template.json` for the dashboard schema.
See `templates/decisions_learnings.template.jsonl` and
`templates/weekly_checkin_log.template.jsonl` for the event schemas.
