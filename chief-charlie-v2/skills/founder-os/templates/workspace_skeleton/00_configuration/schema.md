# Schema

> Auto-generated by Founder-OS onboarding on {{DATE}}.

## Purpose

Quick reference for the founder's data files and where state lives.

## Workspace layout

```
00_configuration/
├── founder_harness.md
└── schema.md

01_command_center/
├── index.md
├── dashboards/
│   ├── dashboard_data.json
│   ├── roadmap_data.json
│   └── roadmap.json, home.json, projects.json, tasks.json, kpis.json  (canvas pills)
└── logs/
    ├── decisions_learnings.jsonl
    ├── weekly_checkin_log.jsonl
    ├── monthly_review_log.jsonl
    ├── quarterly_rebuild_log.jsonl
    ├── pmf_survey_responses.jsonl     (PMF only)
    └── pmf_score_history.jsonl        (PMF only)
```

## Files

| File | Type | Purpose |
|---|---|---|
| `01_command_center/dashboards/dashboard_data.json` | object | Single source of truth for current state |
| `01_command_center/logs/decisions_learnings.jsonl` | append-only stream | All decisions + learnings (universal) |
| `01_command_center/logs/weekly_checkin_log.jsonl` | append-only stream | Check-In history |
| `01_command_center/logs/pmf_survey_responses.jsonl` | append-only stream | PMF only |
| `01_command_center/logs/pmf_score_history.jsonl` | append-only stream | PMF only |

## Read/Write Rules

- **Current state →** read/write `01_command_center/dashboards/dashboard_data.json` only.
- **History →** append to the relevant `01_command_center/logs/*.jsonl`.
- **Never** query `*.jsonl` to find current state. Use `dashboard_data.json`.

## Schemas

See `templates/dashboard_data.template.json` for the dashboard schema.
See `templates/decisions_learnings.template.jsonl` and
`templates/weekly_checkin_log.template.jsonl` for the event schemas.
